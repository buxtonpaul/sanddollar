#!/usr/local/bin/sanddollar
#include <iostream>
#include <string>

int main(int argc, char *argv[])
{
  std::string a;
  std::cout << "Input :" << std::endl;
  std::cin >> a;
  std::cout << "Hello " << a << "\n";
}